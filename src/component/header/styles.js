import React from 'react';
import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  headerStyle: {
    fontSize: 30,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
